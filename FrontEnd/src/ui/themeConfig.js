const themeConfig = {
  token: {
    colorPrimary: "#7ABC87",
  },
  components: {
    Button: {
      contentFontSize: 16,
      contentFontSizeSM: 14,
      borderRadius: 8,
    },
    input: {
      borderRadius: 9,
      borderWidth: 2,
      borderColor: "#D7D7D7",
    },
    select: {
      optionFontSize: 15,
      optionSelectedFontWeight: 700,
    },
  },
};

export default themeConfig; // ✅ Export `themeConfig` directly
